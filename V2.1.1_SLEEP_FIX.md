# v2.1.1 休眠問題修正說明

## 🐛 問題描述

**現象：**
- v2.1 版本的 Streamlit app 無法自動休眠
- 其他版本（v1.0, Gemini 測試版）正常休眠

**影響：**
- 持續消耗 Streamlit Cloud 資源配額
- 無法享受免費版的自動休眠節能機制

---

## 🔍 根本原因分析

### 問題程式碼（v2.1 原版）

**位置：** `app_v2.py` L584-585

```python
# ❌ 問題：每次頁面載入時自動查詢匯率 API
if 'current_rate' not in st.session_state:
    st.session_state['current_rate'] = get_rate_by_date(p['currency_code'], datetime.now().date())
```

### 為何導致無法休眠？

**Streamlit Cloud 的休眠邏輯：**
```
無任何活動 7 天 → 自動休眠
任何 API 呼叫/頁面載入 → 重置休眠計時器
```

**v2.1 的觸發鏈：**
```
Streamlit 健康檢查（定期） 
    ↓
觸發頁面載入
    ↓
執行 init_session() 與 main()
    ↓
檢測 session_state['current_rate'] 不存在
    ↓
呼叫 get_rate_by_date() ← Yahoo Finance API
    ↓
重置休眠計時器 🔄
    ↓
永遠無法達到 7 天無活動
```

### 其他版本為何正常？

**v1.0 與 Gemini 版本：**
```python
# ✅ 正常：僅在使用者操作後才查詢匯率
f_r = get_rate_by_date(p['currency_code'], datetime.now().date())
cur_rate = st.number_input(f"匯率 ({p['currency_code']})", value=float(f_r), step=0.01)
```

- 匯率查詢在 **每次頁面渲染** 時執行
- 但健康檢查 **不會** 觸發完整頁面渲染
- 7 天無使用者訪問 → 正常休眠 ✅

---

## ✅ 解決方案

### 修正後的程式碼（v2.1.1）

```python
# ✅ 修正：使用預設值，避免自動 API 查詢
with c3:
    # 匯率輸入（辨識後會根據收據日期自動更新）
    # 使用預設值避免頁面載入時自動查詢 API（導致無法休眠）
    default_rate = 1.0 if p['currency_code'] == "TWD" else 35.0
    
    cur_rate = st.number_input(
        f"匯率 ({p['currency_code']})", 
        value=float(st.session_state.get('current_rate', default_rate)), 
        step=0.01,
        help="辨識後會根據收據日期自動更新"
    )
```

### 核心改進

| 項目 | 原版 (v2.1) | 修正版 (v2.1.1) |
|-----|------------|----------------|
| 匯率初始值 | 自動查詢 API | 預設值 (TWD=1.0, 其他=35.0) |
| API 呼叫時機 | 每次頁面載入 | 僅辨識時 |
| Session State | 強制初始化 | 使用 `.get()` 方法 |
| 休眠影響 | ❌ 無法休眠 | ✅ 正常休眠 |

---

## 📊 功能影響評估

### ✅ 保留的功能（完全不變）

- [x] 辨識後自動根據收據日期更新匯率
- [x] 匯率查詢準確性
- [x] 所有 OCR 功能
- [x] 地址過濾、金額解析
- [x] 信心度評分

### 🔄 變更的行為

| 情境 | 原版 (v2.1) | 修正版 (v2.1.1) |
|-----|------------|----------------|
| **首次開啟頁面** | 自動顯示當日匯率 | 顯示預設值 35.0 |
| **選擇國家後** | 保持當日匯率 | 保持預設值 35.0 |
| **辨識收據後** | 更新為收據日期匯率 | 更新為收據日期匯率 ✅ |
| **手動修改匯率** | 可修改 | 可修改 ✅ |

### 使用者體驗差異

**原版 (v2.1):**
```
1. 開啟頁面 → 自動顯示 JPY 32.5（當日匯率）
2. 上傳收據 → 更新為 JPY 30.8（2024-01-15 匯率）
```

**修正版 (v2.1.1):**
```
1. 開啟頁面 → 顯示 JPY 35.0（預設值）
2. 上傳收據 → 更新為 JPY 30.8（2024-01-15 匯率）✅ 相同
```

**結論：** 唯一差異是首次開啟頁面時的初始值，但使用者通常會立即上傳收據，因此影響極小。

---

## 🧪 測試驗證

### 測試案例 1：基本功能

**操作步驟：**
1. 開啟 app
2. 選擇國家：日本
3. 檢查匯率顯示

**預期結果：**
| 項目 | v2.1 | v2.1.1 |
|-----|------|--------|
| 初始匯率 | 32.5（當日） | 35.0（預設） |
| 可否手動修改 | ✅ | ✅ |

---

### 測試案例 2：辨識後匯率更新

**操作步驟：**
1. 上傳日本收據（日期：2024-01-15）
2. 執行辨識
3. 檢查匯率欄位

**預期結果：**
| 項目 | v2.1 | v2.1.1 |
|-----|------|--------|
| 辨識後匯率 | 30.8 | 30.8 ✅ |
| 匯率來源 | 2024-01-15 | 2024-01-15 ✅ |

**結論：** 核心功能完全相同

---

### 測試案例 3：休眠機制

**操作步驟：**
1. 部署 v2.1.1
2. 7 天完全不訪問
3. 檢查 app 狀態

**預期結果：**
| 版本 | 狀態 |
|-----|------|
| v2.1 | 🟢 持續運行（無法休眠） |
| v2.1.1 | 🌙 自動休眠 ✅ |

---

## 🚀 部署建議

### 升級步驟

**Step 1: 替換檔案**
```bash
# 下載修正版 app_v2.py
# 上傳至 GitHub 覆蓋原版
git add app_v2.py
git commit -m "fix: Remove auto exchange rate query to enable auto-sleep"
git push
```

**Step 2: Streamlit Cloud 自動更新**
- 無需手動操作
- GitHub 更新後，Streamlit Cloud 自動重新部署
- 等待 2-3 分鐘

**Step 3: 驗證**
```
1. 訪問 app
2. 檢查匯率顯示為預設值（35.0）
3. 上傳收據測試自動更新功能
```

---

## 📊 版本對照

| 版本 | 發布日期 | 核心改進 | 已知問題 |
|-----|---------|---------|---------|
| v1.0 | 2025-01 | 原始版本 | 準確度較低 |
| v2.0 | 2025-02-11 | OCR 準確度 +30% | - |
| v2.1 | 2025-02-11 | 地址/金額/匯率修正 | ⚠️ 無法休眠 |
| **v2.1.1** | **2025-02-12** | **修正休眠問題** | **無** ✅ |

---

## 💡 技術洞察

### Streamlit Session State 最佳實踐

**❌ 不建議（會導致問題）：**
```python
# 在頁面載入時強制初始化
if 'key' not in st.session_state:
    st.session_state['key'] = expensive_api_call()  # ← 每次載入都執行
```

**✅ 建議：**
```python
# 使用 .get() 提供預設值
value = st.session_state.get('key', default_value)  # ← 不觸發 API
```

### Streamlit Cloud 休眠觸發器

**會重置休眠計時器的活動：**
- ✅ 使用者訪問
- ✅ API 呼叫（包括 Yahoo Finance, Google Sheets）
- ✅ 健康檢查觸發的頁面載入

**不會重置的活動：**
- ⬜ 靜態檔案快取
- ⬜ Session state 讀取（非寫入）
- ⬜ 輕量級健康檢查（不觸發完整渲染）

---

## 🎯 總結

### 問題
v2.1 因匯率自動查詢導致無法休眠

### 解決
移除自動查詢，使用預設值 + 辨識時更新

### 影響
- ✅ 核心功能完全不變
- ✅ 可正常休眠節省資源
- ⚠️ 首次開啟顯示預設值（影響極小）

### 建議
立即升級至 v2.1.1

---

**版本：** v2.1.1  
**更新日期：** 2025-02-12  
**修正問題：** Streamlit Cloud 無法休眠
