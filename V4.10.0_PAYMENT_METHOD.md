# v4.10.0 新功能：支付方式選擇與同步優化

## 🎯 更新內容

### 功能 1: 支付方式選擇（現金 / 信用卡）⭐⭐⭐

**新增位置：** 頂部導航列第三欄

#### 使用情境

##### 情境 A：信用卡支付（預設）
```
使用者操作：
1. 選擇「信用卡」
2. 設定手續費率（預設 1.5%）
3. 上傳收據並辨識

系統行為：
- AI 辨識 → 自動查詢匯率
- 計算：外幣金額 × 匯率 = 台幣基礎金額
- 手續費：台幣基礎 × 1.5% = 手續費
- 總計：台幣基礎 + 手續費

匯率來源：Yahoo Finance API（官方匯率）
```

**範例：**
```
收據：€100
匯率：34.5（自動查詢）
台幣基礎：3,450
手續費（1.5%）：52
總計：3,502
```

---

##### 情境 B：現金兌換
```
使用者操作：
1. 選擇「現金」
2. 上傳收據並辨識
3. 在編輯表單中手動調整匯率

系統行為：
- AI 辨識 → 自動查詢匯率（僅作參考）
- 使用者可手動修改為實際兌換匯率
- 計算：外幣金額 × 實際匯率 = 台幣金額
- 無手續費

匯率來源：使用者手動輸入（實際兌換匯率）
```

**範例：**
```
收據：€100
系統匯率：34.5（參考）
實際匯率：34.2（手動輸入，含兌換損失）
台幣金額：3,420
手續費：0
總計：3,420
```

---

### 功能 2: 編輯表單支付方式切換

**卡片編輯介面新增：**

#### 信用卡模式
```
┌────────────────────────────────┐
│ 商店名稱：[          ]         │
│ 日期：[日曆]                   │
│ 外幣金額：[          ]         │
│ 幣別：[          ]             │
│ 💳 支付方式：[信用卡 ▼]        │  ← 可切換
│ 💱 匯率：[34.500]              │  ← 3 位小數
│    （信用卡匯率，可微調）      │
│ 品項摘要：[          ]         │
│ 備註：[          ]             │
│                                │
│ 💳 信用卡 | 💰 台幣：3,450     │
│                                │
│ [✅ 更新]                      │
└────────────────────────────────┘
```

#### 現金模式
```
┌────────────────────────────────┐
│ 商店名稱：[          ]         │
│ 日期：[日曆]                   │
│ 外幣金額：[          ]         │
│ 幣別：[          ]             │
│ 💳 支付方式：[現金 ▼]          │  ← 可切換
│ 💱 實際兌換匯率：[34.2000]     │  ← 4 位小數
│    （現金兌換時的實際匯率）    │
│ 品項摘要：[          ]         │
│ 備註：[          ]             │
│                                │
│ 💵 現金兌換 | 💰 台幣：3,420   │
│                                │
│ [✅ 更新]                      │
└────────────────────────────────┘
```

---

### 功能 3: 匯率更新邏輯優化

#### 三種模式的匯率處理

##### 模式 A：現金模式
```python
規則：
1. 使用者手動輸入優先
2. 不自動查詢匯率
3. 支援 4 位小數（更精確）

流程：
使用者修改匯率 → 直接使用新值 → 重新計算台幣
```

##### 模式 B：信用卡模式 - 手動調整
```python
規則：
1. 使用者手動修改 → 使用新值
2. 不自動重新查詢

流程：
使用者修改匯率 → 使用新值 → 重新計算台幣
```

##### 模式 C：信用卡模式 - 自動查詢
```python
規則：
1. 日期變更 → 自動查詢新匯率
2. 幣別變更 → 自動查詢新匯率
3. 使用 3 位小數

流程：
使用者修改日期/幣別 → 自動查詢匯率 → 重新計算台幣
```

---

### 功能 4: 同步邏輯優化

#### 問題分析

**原版問題：**
```
1. 同步後自動清空暫存區
   ↓
2. 使用者無法檢查同步結果
   ↓
3. 如果需要修正，必須重新上傳
```

**優化後：**
```
1. 同步完成
   ↓
2. 暫存區保留資料
   ↓
3. 使用者可以：
   - 檢查同步結果
   - 繼續修改資料
   - 再次同步（更新）
   ↓
4. 確認無誤後，手動清空暫存區
```

---

#### 新增清空按鈕

**位置：** 同步按鈕下方

```
┌────────────────────────────────┐
│                                │
│ [📤 同步至雲端]                │
│                                │
│ ✅ 同步完成！新增 3 筆         │
│ 💡 暫存區資料仍保留           │
│                                │
│ ────────────────────────       │
│                                │
│ [🗑️ 清空暫存區] [說明文字]    │
│                                │
└────────────────────────────────┘
```

---

## 📊 使用流程對比

### 信用卡流程

```
1. 選擇「信用卡」
   ↓
2. 設定手續費率（1.5%）
   ↓
3. 上傳收據
   ↓
4. AI 辨識
   - 自動查詢匯率 ✓
   ↓
5. 確認 / 微調
   - 可微調匯率
   ↓
6. 同步至雲端
   - 台幣金額含手續費
   ↓
7. 檢查結果
   ↓
8. 手動清空暫存區
```

---

### 現金流程

```
1. 選擇「現金」
   ↓
2. 上傳收據
   ↓
3. AI 辨識
   - 提供參考匯率
   ↓
4. 手動輸入實際匯率
   - 4 位小數精確度
   - 反映實際兌換率
   ↓
5. 確認其他資訊
   ↓
6. 同步至雲端
   - 台幣金額無手續費
   ↓
7. 檢查結果
   ↓
8. 手動清空暫存區
```

---

## 🎯 實際使用案例

### 案例 1: 信用卡海外消費

**情境：**
- 在日本用信用卡消費 ¥10,000
- 信用卡收 1.5% 海外手續費

**操作：**
```
1. 選擇「信用卡」
2. 手續費率：1.5%
3. 上傳收據
4. AI 辨識：
   - 金額：¥10,000
   - 匯率：0.220（自動查詢）
5. 確認資料
6. 同步

結果：
- 台幣基礎：2,200
- 手續費：33
- 總計：2,233 ✓
```

---

### 案例 2: 機場現金兌換

**情境：**
- 在機場兌換 €500
- 實際匯率 33.8（含兌換損失）

**操作：**
```
1. 選擇「現金」
2. 上傳收據（如有）或手動建立
3. AI 辨識：
   - 金額：€500
   - 匯率：34.5（系統參考值）
4. 修正匯率：
   - 手動改為：33.8000
5. 確認資料
6. 同步

結果：
- 台幣：16,900
- 手續費：0
- 總計：16,900 ✓
```

---

### 案例 3: 混合使用

**情境：**
- 同一趟旅行，有現金也有信用卡

**操作：**
```
第一批（信用卡）：
1. 選擇「信用卡」
2. 上傳 5 張收據
3. 辨識 → 同步
4. 不清空暫存區

第二批（現金）：
5. 切換為「現金」
6. 上傳 3 張收據
7. 辨識 → 手動調整匯率
8. 同步（會合併到同一 Sheet）
9. 確認後清空暫存區

結果：
- Sheet 有 8 筆記錄
- 5 筆標註「信用卡」
- 3 筆標註「現金」
```

---

## 🔧 技術實作

### 資料結構

```python
record = {
    "UID": "abc123def456",
    "商店名稱": "7-11",
    "日期": "2024-06-27",
    "外幣金額": 1000,
    "幣別": "JPY",
    "匯率": 0.2200,  # 信用卡：3位小數；現金：4位小數
    "台幣金額": 220,
    "品項摘要": "飲料、零食",
    "備註": "",
    "支付方式": "信用卡"  # 新增欄位
}
```

---

### 匯率精度

```python
# 信用卡模式
rate_credit = round(exchange_rate, 3)  # 0.220
step = 0.001

# 現金模式
rate_cash = round(exchange_rate, 4)  # 0.2185
step = 0.0001
```

---

### 條件性欄位顯示

```python
if payment_method == "現金":
    # 顯示實際兌換匯率欄位（4位小數）
    new_rate = st.number_input(
        "💱 實際兌換匯率", 
        format="%.4f", 
        step=0.0001
    )
else:
    # 顯示信用卡匯率欄位（3位小數）
    new_rate = st.number_input(
        "💱 匯率", 
        format="%.3f", 
        step=0.001
    )
```

---

## ⚠️ 注意事項

### 1. 支付方式不可溯改同步資料

**問題：**
已同步的資料沒有「支付方式」欄位

**解決：**
- 新辨識的資料會包含支付方式
- 舊資料預設為「信用卡」
- 可在編輯時手動切換

---

### 2. 現金匯率需手動輸入

**重要：**
- 系統提供的匯率只是「參考」
- 現金模式必須手動調整為實際匯率
- 否則會使用錯誤的匯率計算

---

### 3. 暫存區不會自動清空

**新行為：**
- 同步完成後，資料保留在暫存區
- 使用者可以繼續修改或檢查
- 確認無誤後，手動點擊「清空暫存區」

**好處：**
- 可以檢查同步結果
- 發現錯誤可立即修正
- 避免資料遺失

---

## 📋 測試清單

### 測試 1: 信用卡模式
```
□ 選擇「信用卡」
□ 設定手續費 1.5%
□ 上傳收據
□ 確認匯率自動查詢
□ 確認台幣含手續費
□ 同步成功
□ 暫存區保留
```

---

### 測試 2: 現金模式
```
□ 選擇「現金」
□ 上傳收據
□ 手動調整匯率（4位小數）
□ 確認台幣無手續費
□ 同步成功
□ 暫存區保留
```

---

### 測試 3: 模式切換
```
□ 在編輯表單切換支付方式
□ 確認匯率欄位文字變更
□ 確認精度變更（3位 ↔ 4位）
□ 確認台幣計算正確
```

---

### 測試 4: 同步與清空
```
□ 同步資料
□ 確認暫存區未清空
□ 檢查雲端資料正確
□ 手動清空暫存區
□ 確認暫存區已清空
```

---

## 🎉 總結

### v4.10.0 核心價值

1. **支付方式選擇**
   - 信用卡 / 現金分開處理
   - 匯率精度不同
   - 手續費條件性套用

2. **匯率靈活性**
   - 現金：手動輸入實際匯率
   - 信用卡：自動查詢可微調

3. **同步優化**
   - 不自動清空暫存區
   - 使用者主動控制
   - 避免資料遺失

---

**版本：** v4.10.0  
**發布日期：** 2026-02-23  
**核心功能：** 支付方式選擇、匯率精度優化、同步流程改善
