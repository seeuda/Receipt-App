# v4.9.4 UID 邏輯修正與去重強化

## 🐛 問題診斷

### 問題 1: UID 包含使用者名稱

**原版邏輯：**
```python
uid = hashlib.md5(img_bytes + f_user.encode()).hexdigest()[:12]
```

**問題：**
```
張三上傳收據 A → UID = abc123def456
李四上傳同一張收據 A → UID = xyz789ghi012

結果：同一張收據產生兩筆記錄 ❌
```

**設計原意：**
- 同一張收據，無論誰上傳，都應該是同一個 UID
- 後上傳的應該**覆蓋**前面的記錄（包含時間戳和使用者）

---

### 問題 2: 一次上傳選到重複照片

**場景：**
使用者在檔案選擇器中：
1. 選擇 receipt_A.jpg
2. 按住 Ctrl 再選一次 receipt_A.jpg
3. 點擊「開啟」

**原版結果：**
```
batch = [
  {UID: "abc123", 商店: "7-11", 金額: 100},
  {UID: "abc123", 商店: "7-11", 金額: 100}  ← 重複！
]

暫存區：兩筆相同的收據 ❌
同步後：雲端出現兩列相同 UID ❌
```

**設計原意：**
- 批次內部就應該去重
- 即使選到重複照片，也只保留一筆

---

## ✅ 解決方案

### 修正 1: UID 只依據圖片內容

**修正前：**
```python
uid = hashlib.md5(img_bytes + f_user.encode()).hexdigest()[:12]
```

**修正後：**
```python
# UID 只依據圖片內容（不包含使用者名稱）
# 確保同一張照片無論誰上傳都是相同 UID
uid = hashlib.md5(img_bytes).hexdigest()[:12]
```

**效果：**
```
張三上傳收據 A → UID = abc123def456
李四上傳同一張收據 A → UID = abc123def456 ✅（相同）

雲端記錄：
- 第一次（張三上傳）：
  時間: 2024-06-27 10:00
  使用者: 張三
  UID: abc123def456

- 第二次（李四上傳）：
  時間: 2024-06-28 15:00  ← 覆蓋
  使用者: 李四              ← 覆蓋
  UID: abc123def456        ← 相同，覆蓋同一列
```

---

### 修正 2: 三層去重機制

#### 層級 1: 批次內部去重

**場景：** 同一次上傳選到重複照片

```python
# 步驟 1: 批次內部去重
batch_uids = {}
deduplicated_batch = []
duplicate_count = 0

for record in batch:
    uid = record['UID']
    if uid not in batch_uids:
        batch_uids[uid] = record
        deduplicated_batch.append(record)
    else:
        duplicate_count += 1  # 統計重複數量

if duplicate_count > 0:
    st.info(f"ℹ️ 過濾掉 {duplicate_count} 張重複照片（同一次上傳內）")
```

**效果：**
```
上傳時選了：[A, B, A, C, B]
批次去重後：[A, B, C] ✅
顯示訊息：「過濾掉 2 張重複照片」
```

---

#### 層級 2: 與暫存區去重

**場景：** 新上傳的照片已存在暫存區

```python
# 步驟 2: 與暫存區去重
existing_uids = {record['UID'] for record in st.session_state['data']}
new_records = [record for record in deduplicated_batch if record['UID'] not in existing_uids]
```

**效果：**
```
暫存區現有：[A, B, C]
新上傳：[C, D, E]（已批次去重）
比對後：D, E 是新的
結果：只新增 [D, E] ✅
暫存區變成：[A, B, C, D, E]
```

---

#### 層級 3: 雲端同步時覆蓋

**場景：** 同步時檢查雲端是否已有相同 UID

```python
# 建立雲端 UID -> 列號對照表
uid_col = wks.col_values(13)  # M 欄
uid_to_row = {}
for idx, uid in enumerate(uid_col, start=1):
    if uid:
        uid_to_row[uid] = idx

# 逐筆檢查
for r in st.session_state['data']:
    uid = r['UID']
    
    if uid in uid_to_row:
        # UID 已存在 → 覆蓋該列
        rownum = uid_to_row[uid]
        updates.append({
            "range": f"A{rownum}:L{rownum}",
            "values": [row_data]
        })
    else:
        # UID 不存在 → 新增
        rows_to_append.append(row_data + [uid])
```

**效果：**
```
雲端現有：
列 3: UID=abc123, 商店=7-11, 使用者=張三, 時間=10:00
列 4: UID=def456, 商店=全家, 使用者=李四, 時間=11:00

暫存區要同步：
[{UID=abc123, 商店=7-11, 使用者=王五, 時間=15:00}]

結果：
列 3: UID=abc123, 商店=7-11, 使用者=王五, 時間=15:00 ✅（覆蓋）
列 4: 不變
```

---

## 📊 完整流程圖

### 場景：張三和李四分別上傳同一張收據

```
=== 第一次：張三上傳 ===

1. 張三選擇照片 A
   ↓
2. 生成 UID
   img_bytes → MD5 → abc123def456
   ↓
3. AI 辨識
   商店: 7-11, 金額: 100, 日期: 2024-06-27
   ↓
4. 批次去重（第一層）
   batch = [{UID: abc123, 商店: 7-11}]
   沒有批次內重複 ✓
   ↓
5. 暫存區去重（第二層）
   existing_uids = {}（暫存區是空的）
   new_records = [abc123]
   ↓
6. 加入暫存區
   data = [{UID: abc123, 商店: 7-11, 使用者: 張三}]
   ↓
7. 張三點擊「同步至雲端」
   ↓
8. 雲端覆蓋檢查（第三層）
   uid_to_row = {}（雲端是空的）
   → 新增一列
   ↓
9. 雲端結果
   列 3: 記錄時間=10:00, 使用者=張三, UID=abc123

===================================

=== 第二次：李四上傳同一張照片 A ===

1. 李四選擇照片 A（與張三的是同一個檔案）
   ↓
2. 生成 UID
   img_bytes → MD5 → abc123def456 ✅（相同！）
   ↓
3. AI 辨識
   商店: 7-11, 金額: 100, 日期: 2024-06-27
   ↓
4. 批次去重（第一層）
   batch = [{UID: abc123, 商店: 7-11}]
   沒有批次內重複 ✓
   ↓
5. 暫存區去重（第二層）
   existing_uids = {}（李四的暫存區是空的）
   new_records = [abc123]
   ↓
6. 加入暫存區
   data = [{UID: abc123, 商店: 7-11, 使用者: 李四}]
   ↓
7. 李四點擊「同步至雲端」
   ↓
8. 雲端覆蓋檢查（第三層）
   uid_to_row = {abc123: 3}（已存在於第 3 列）
   → 覆蓋第 3 列 ✅
   ↓
9. 雲端結果
   列 3: 記錄時間=15:00, 使用者=李四, UID=abc123
   （張三的記錄被覆蓋）
```

---

## 🧪 測試案例

### 測試 1: 不同人上傳同照片

```
操作：
1. 張三登入，上傳 receipt_A.jpg
2. 李四登入，上傳同一個 receipt_A.jpg

預期結果：
- 張三辨識：UID = abc123
- 李四辨識：UID = abc123 ✅（相同）
- 雲端只有一列（李四覆蓋張三）✅
```

---

### 測試 2: 一次上傳選到重複照片

```
操作：
1. 在檔案選擇器選擇：[A, B, A, C]
2. 點擊上傳

預期結果：
- 批次內部去重：[A, B, C] ✅
- 顯示訊息：「過濾掉 1 張重複照片」✅
- 暫存區：3 筆（不是 4 筆）✅
```

---

### 測試 3: 多次上傳同照片

```
操作：
1. 第一次上傳 A → 暫存區：[A]
2. 第二次上傳 A → 暫存區：[A]（不變）
3. 顯示警告：「所有收據都已存在」✅

結果：
- 暫存區始終只有 1 筆 ✅
- 不會重複辨識 ✅
```

---

### 測試 4: 覆蓋雲端資料

```
操作：
1. 張三上傳 A 並同步
   → 雲端：列 3 = {UID: abc123, 使用者: 張三, 時間: 10:00}

2. 李四上傳同一個 A 並同步
   → 雲端：列 3 = {UID: abc123, 使用者: 李四, 時間: 15:00}

預期結果：
- 列 3 被覆蓋 ✅
- 沒有新增列 4 ✅
- 最新資訊是李四的 ✅
```

---

## 🎯 設計原理

### UID 的唯一性來源

**只依據圖片內容：**
```python
uid = hashlib.md5(img_bytes).hexdigest()[:12]
```

**為什麼不包含使用者？**
1. 同一張收據應該只有一筆記錄
2. 後上傳的應該覆蓋舊的（含時間戳和使用者）
3. 避免重複記帳

**為什麼不包含日期/金額？**
- 這些是辨識結果，可能有誤差
- 照片內容才是唯一不變的

---

### 覆蓋 vs 新增的判斷

**規則：**
```
if UID 已存在於雲端:
    → 覆蓋（update）
    → 更新時間戳和使用者
    → 更新辨識結果（商店、金額、日期等）
else:
    → 新增（append）
```

**原因：**
- 可能是重複上傳
- 可能是不同人上傳同一張收據
- 可能是辨識錯誤後重新上傳
- 保留最新的辨識結果

---

## ⚠️ 注意事項

### 1. 照片修改

**如果使用者編輯照片：**
```
原始照片 A → UID = abc123
裁切後的照片 A' → UID = xyz789（不同！）

結果：視為不同收據 ✓
```

這是合理的，因為圖片內容已改變。

---

### 2. 檔案格式轉換

**相同內容，不同格式：**
```
photo.jpg → UID = abc123
photo.png（轉換自 jpg）→ UID = def456（不同）

結果：視為不同收據 ✓
```

這也是合理的，因為 bytes 內容不同。

---

### 3. UID 碰撞機率

**MD5 前 12 字元：**
- 可能性：16^12 = 2.8×10^14
- 碰撞機率：< 0.00000001%

實務上可忽略不計。

---

## 🎉 總結

### v4.9.4 修正內容

1. **UID 只依據圖片**
   - 移除使用者名稱
   - 同一張照片 = 同一個 UID

2. **三層去重機制**
   - 批次內部去重
   - 與暫存區去重
   - 雲端同步時覆蓋

3. **覆蓋邏輯**
   - 同 UID 會更新資料
   - 時間戳和使用者會更新
   - 保留最新的辨識結果

---

**版本：** v4.9.4  
**發布日期：** 2025-02-13  
**核心功能：** UID 邏輯修正、去重強化、覆蓋機制
