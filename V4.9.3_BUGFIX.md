# v4.9.3 Bug 修正：日期格式與資料去重

## 🐛 問題診斷

### 問題 1: 日期格式錯誤

**錯誤訊息：**
```python
ValueError: time data %r does not match format %r
File "app_enhanced.py", line 363
    new_date = st.date_input("日期", value=datetime.strptime(record['日期'], "%Y-%m-%d").date())
```

**原因：**
- AI 辨識回傳的日期格式不穩定
- 可能回傳 "2024-06-26" 或 "06/26/24" 或其他格式
- 直接使用 `strptime` 會導致錯誤

---

### 問題 2: 資料重複

**現象：**
- 使用者上傳 3 張收據
- 辨識後暫存區顯示 3 筆 ✓
- 再上傳 2 張收據
- 辨識後暫存區顯示 5 筆... 但應該是 5 筆新資料，不是累加

**原因：**
```python
# 原版：直接覆蓋
st.session_state['data'] = batch

# 問題：
# 第一次上傳 3 張 → data = [A, B, C]
# 第二次上傳 2 張 → data = [D, E]  ← C 消失了！
```

**實際需求：**
- 累加新資料
- 但避免重複（同一張收據上傳兩次）

---

## ✅ 解決方案

### 修正 1: 日期格式驗證與容錯

#### 辨識階段（Line 303-325）

**加入日期驗證：**
```python
res = run_vlm_scan(active_key, img_bytes, target_year, target_country)
if res:
    # 確保日期格式正確
    try:
        receipt_date = res['date']
        datetime.strptime(receipt_date, "%Y-%m-%d")  # 驗證格式
    except:
        # 格式錯誤，使用當天日期並警告
        receipt_date = datetime.now().strftime("%Y-%m-%d")
        st.warning(f"⚠️ 收據日期格式錯誤，已設為今天：{receipt_date}")
    
    batch.append({
        "日期": receipt_date,  # 使用驗證過的日期
        ...
    })
```

---

#### 顯示階段（Line 363）

**加入容錯處理：**
```python
# 原版（會崩潰）
new_date = st.date_input("日期", value=datetime.strptime(record['日期'], "%Y-%m-%d").date())

# 修正版（有容錯）
try:
    if isinstance(record['日期'], str):
        date_value = datetime.strptime(record['日期'], "%Y-%m-%d").date()
    else:
        date_value = record['日期']
except:
    # 格式錯誤，使用當天日期
    date_value = datetime.now().date()
    st.warning(f"⚠️ 日期格式錯誤，已設為今天：{date_value}")

new_date = st.date_input("日期", value=date_value)
```

---

### 修正 2: 資料去重與累加

#### 原版（有問題）

```python
if batch:
    st.session_state['data'] = batch  # 直接覆蓋
    st.session_state['uploaded_images'] = images
    st.rerun()
```

**問題：**
```
第一次：data = [A, B, C]
第二次：data = [D, E]  ← A, B, C 不見了！
```

---

#### 修正版（使用 UID 去重）

```python
if batch:
    # 取得現有的 UID 集合
    existing_uids = {record['UID'] for record in st.session_state['data']}
    
    # 只保留新的資料（UID 不重複）
    new_records = [record for record in batch if record['UID'] not in existing_uids]
    
    if new_records:
        # 附加新資料（而非覆蓋）
        st.session_state['data'].extend(new_records)
        # 更新圖片
        st.session_state['uploaded_images'].update(images)
        st.success(f"✅ 新增 {len(new_records)} 筆辨識結果")
    else:
        st.warning("⚠️ 所有收據都已存在，未新增任何資料")
    
    st.rerun()
```

**效果：**
```
第一次上傳 3 張：
- batch = [A, B, C]
- existing_uids = {}
- new_records = [A, B, C]
- data = [] → [A, B, C] ✅

第二次上傳 2 張新收據：
- batch = [D, E]
- existing_uids = {uid_A, uid_B, uid_C}
- new_records = [D, E]
- data = [A, B, C] → [A, B, C, D, E] ✅

第三次重複上傳 A：
- batch = [A]
- existing_uids = {uid_A, uid_B, uid_C, uid_D, uid_E}
- new_records = []
- 顯示警告：所有收據都已存在 ✅
```

---

## 📊 測試案例

### 測試 1: 正常日期格式

```
輸入：
- AI 回傳：{"date": "2024-06-26", ...}

預期：
- 驗證通過 ✅
- 正常儲存
- 顯示正常
```

---

### 測試 2: 錯誤日期格式

```
輸入：
- AI 回傳：{"date": "06/26/24", ...}

預期：
- 驗證失敗
- 自動改為今天日期
- 顯示警告訊息 ⚠️
```

---

### 測試 3: 累加新資料

```
操作：
1. 上傳 3 張收據（A, B, C）
   → 暫存區：[A, B, C] ✅

2. 上傳 2 張新收據（D, E）
   → 暫存區：[A, B, C, D, E] ✅

3. 查看暫存區
   → 顯示 5 筆完整資料 ✅
```

---

### 測試 4: 重複上傳去重

```
操作：
1. 上傳收據 A
   → 暫存區：[A] ✅

2. 再次上傳相同的收據 A
   → 暫存區：[A] ✅
   → 顯示：「所有收據都已存在」⚠️

3. 上傳新收據 B
   → 暫存區：[A, B] ✅
```

---

## 🎯 UID 計算方式

```python
uid = hashlib.md5(img_bytes + f_user.encode()).hexdigest()[:12]
```

**說明：**
- 使用圖片內容 + 使用者名稱計算 MD5
- 取前 12 個字元作為 UID
- 同一張收據 + 同一個使用者 = 相同 UID
- 不同圖片或不同使用者 = 不同 UID

**效果：**
- ✅ 同一張收據重複上傳會被去重
- ✅ 不同收據一定有不同 UID
- ✅ 不同使用者上傳同一張收據會產生不同 UID（合理）

---

## 🔧 完整流程

### 使用者操作流程

```
1. 上傳 3 張收據
   ↓
2. AI 辨識
   - 驗證日期格式
   - 生成 UID
   - 建立 batch = [A, B, C]
   ↓
3. 去重檢查
   - existing_uids = {}（第一次為空）
   - new_records = [A, B, C]
   ↓
4. 更新暫存區
   - data.extend([A, B, C])
   - data = [A, B, C]
   ↓
5. 使用者繼續上傳 2 張新收據
   ↓
6. AI 辨識
   - 建立 batch = [D, E]
   ↓
7. 去重檢查
   - existing_uids = {uid_A, uid_B, uid_C}
   - new_records = [D, E]（D 和 E 不在 existing_uids 中）
   ↓
8. 更新暫存區
   - data.extend([D, E])
   - data = [A, B, C, D, E]
   ↓
9. 使用者查看暫存區
   - 顯示 5 筆收據 ✅
   - 可個別編輯
   - 可批次同步
```

---

## ⚠️ 注意事項

### 1. 清空暫存區

如果使用者想重新開始：
```
點擊 Sidebar 的「清空辨識紀錄」按鈕
→ st.session_state['data'] = []
→ st.session_state['uploaded_images'] = {}
```

---

### 2. UID 衝突

**極低機率：**
- MD5 前 12 字元 = 16^12 種組合
- 碰撞機率 < 0.0000001%

**如果真的發生：**
- 系統會認為是重複收據
- 不會新增
- 使用者可以手動調整圖片（例如裁切 1px）重新上傳

---

### 3. 日期格式

**支援的格式：**
- ✅ "2024-06-26"（標準格式）

**不支援的格式：**
- ❌ "06/26/24"
- ❌ "26-06-2024"
- ❌ "2024年6月26日"

**處理方式：**
- 自動轉換為當天日期
- 顯示警告訊息
- 使用者可手動修正

---

## 🎉 總結

### v4.9.3 修正內容

1. **日期格式驗證**
   - 辨識階段驗證
   - 顯示階段容錯
   - 自動回退到當天日期

2. **資料去重機制**
   - 使用 UID 識別
   - 自動過濾重複
   - 累加新資料

3. **使用者體驗**
   - 顯示新增筆數
   - 重複上傳警告
   - 錯誤訊息清晰

---

**版本：** v4.9.3  
**發布日期：** 2025-02-13  
**核心功能：** 日期容錯、資料去重、使用者體驗優化
