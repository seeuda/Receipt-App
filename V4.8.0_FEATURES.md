# v4.8.0 功能增強說明

## 🎉 新增功能

### 1. 辨識結果表格增強

**新增欄位：**
- ✅ 匯率（根據收據日期自動查詢）
- ✅ 台幣金額（自動計算）

**表格結構：**
```
| UID | 商店名稱 | 日期 | 外幣金額 | 幣別 | 匯率 | 台幣金額 | 品項摘要 | 備註 |
```

---

### 2. 智慧匯率查詢

**功能：根據收據日期自動抓取當天匯率**

#### 邏輯流程

```
收據日期 (例：2025-02-15)
    ↓
檢查是否為週末
    ↓ 是週六/日
往前推至週五 (2025-02-14)
    ↓
查詢該日匯率
    ↓
如無資料，繼續往前推
    ↓
最多往前推 7 天
    ↓
若都無資料，使用最新匯率
```

#### 實作細節

```python
def get_rate_by_date(currency_code, target_date):
    """根據日期查詢匯率（週末自動往前抓週五）"""
    if currency_code == "TWD":
        return 1.0
    
    # 將日期往前推到工作日
    check_date = target_date
    max_attempts = 7
    
    for _ in range(max_attempts):
        # 如果是週末（5=六, 6=日），往前推
        while check_date.weekday() >= 5:
            check_date -= timedelta(days=1)
        
        # 查詢該日期的匯率
        ticker = yf.Ticker(f"{currency_code}TWD=X")
        hist = ticker.history(start=check_date, end=check_date + timedelta(days=1))
        
        if not hist.empty:
            return float(hist['Close'].iloc[0])
        
        check_date -= timedelta(days=1)
    
    # 預設值
    return 35.0
```

#### 測試案例

| 收據日期 | 星期 | 查詢日期 | 說明 |
|---------|------|---------|------|
| 2025-02-15 | 六 | 2025-02-14 (五) | 週六→往前到週五 |
| 2025-02-16 | 日 | 2025-02-14 (五) | 週日→往前到週五 |
| 2025-02-17 | 一 | 2025-02-17 (一) | 工作日→直接查詢 |
| 2025-12-25 | 三 | 2025-12-24 (二) | 聖誕節假日→往前 |

---

### 3. 即時連動計算

**觸發條件：**
使用者修改以下任一欄位時，自動重新計算：
- 日期
- 外幣金額
- 幣別

**重新計算流程：**
```
使用者修改日期/金額/幣別
    ↓
偵測變更
    ↓
重新查詢匯率（根據新日期）
    ↓
更新匯率欄位
    ↓
重新計算台幣金額
    ↓
即時顯示結果
```

#### 實作細節

```python
# 偵測變更並重新計算
if not edited_df.equals(df):
    for idx, row in edited_df.iterrows():
        old_row = df.loc[idx]
        
        # 檢查是否有變更
        if (row['日期'] != old_row['日期'] or 
            row['外幣金額'] != old_row['外幣金額'] or
            row['幣別'] != old_row['幣別']):
            
            # 重新查詢匯率
            new_rate = get_rate_by_date(row['幣別'], row['日期'])
            edited_df.at[idx, '匯率'] = round(new_rate, 3)
            edited_df.at[idx, '台幣金額'] = round(row['外幣金額'] * new_rate, 0)
    
    # 更新並重新整理
    st.session_state['data'] = edited_df.to_dict('records')
    st.rerun()
```

---

### 4. 收據照片預覽

**佈局：**
```
┌─────────────────────────┬──────────────────┐
│  辨識結果表格            │  收據預覽        │
│  (可編輯)               │                  │
│                         │  [選擇收據下拉]  │
│  ├ UID                  │                  │
│  ├ 商店名稱             │  ┌─────────────┐│
│  ├ 日期                 │  │             ││
│  ├ 外幣金額             │  │   收據圖片   ││
│  ├ 幣別                 │  │             ││
│  ├ 匯率                 │  │             ││
│  ├ 台幣金額             │  └─────────────┘│
│  ├ 品項摘要             │                  │
│  └ 備註                 │  商店 - 日期     │
│                         │                  │
└─────────────────────────┴──────────────────┘
```

**功能：**
- ✅ 下拉選單選擇要預覽的收據
- ✅ 即時顯示對應的收據圖片
- ✅ 圖片自動縮放適應視窗
- ✅ 顯示圖片說明（商店名稱 - 日期）

#### 實作細節

```python
# 儲存圖片（辨識時）
images[uid] = base64.b64encode(img_bytes).decode()

# 顯示預覽
selected_uid = edited_df.iloc[selected_idx]['UID']
if selected_uid in st.session_state['uploaded_images']:
    img_data = base64.b64decode(st.session_state['uploaded_images'][selected_uid])
    st.image(img_data, caption=f"收據 - {preview_options[selected_idx]}")
```

---

## 📊 使用流程

### 完整操作範例

#### Step 1: 上傳收據

```
1. 選擇國家：日本
2. 上傳收據照片：receipt_tokyo.jpg
3. 點擊「啟動 AI 自動偵察」
```

#### Step 2: 自動辨識

```
辨識結果：
- 商店名稱：ファミリーマート
- 日期：2025-02-15（週六）
- 外幣金額：¥1,500
- 幣別：JPY
```

#### Step 3: 自動查詢匯率

```
日期：2025-02-15（週六）
  ↓
往前推至：2025-02-14（週五）
  ↓
查詢匯率：0.220
  ↓
計算台幣：1,500 × 0.220 = 330 元
```

#### Step 4: 結果顯示

```
表格顯示：
| 商店名稱      | 日期       | 外幣金額 | 幣別 | 匯率  | 台幣金額 |
|--------------|-----------|---------|------|-------|---------|
| ファミリー... | 2025-02-15| 1500.00 | JPY  | 0.220 | 330     |
```

#### Step 5: 手動修正（如需要）

```
使用者發現金額有誤：
- 修改外幣金額：1,500 → 1,800
```

**系統自動反應：**
```
偵測到金額變更
  ↓
保持原匯率：0.220
  ↓
重新計算：1,800 × 0.220 = 396
  ↓
即時更新台幣金額：330 → 396
```

#### Step 6: 預覽確認

```
右側預覽區：
- 選擇：ファミリーマート - 2025-02-15
- 顯示：收據照片
- 比對：確認金額是否正確
```

#### Step 7: 同步雲端

```
確認無誤後：
- 點擊「同步至雲端」
- 寫入 Google Sheets
- 清空辨識紀錄
```

---

## 🎯 核心改進

### 改進 1: 匯率準確性

**問題（舊版）：**
```
❌ 同步時才查詢匯率
❌ 使用當下匯率，非收據日期匯率
❌ 週末查詢失敗
```

**解決（新版）：**
```
✅ 辨識完成即查詢匯率
✅ 根據收據日期查詢歷史匯率
✅ 週末自動往前抓週五匯率
```

---

### 改進 2: 使用者體驗

**問題（舊版）：**
```
❌ 無法看到台幣金額
❌ 修正金額需手動計算
❌ 無法預覽照片
```

**解決（新版）：**
```
✅ 表格直接顯示台幣金額
✅ 修正後自動重新計算
✅ 可預覽對應的收據照片
```

---

### 改進 3: 資料完整性

**新增欄位：**
```
舊版：UID, 商店, 日期, 外幣金額, 幣別, 品項, 備註
新版：UID, 商店, 日期, 外幣金額, 幣別, 匯率, 台幣金額, 品項, 備註
                                        ↑      ↑
                                      新增    新增
```

---

## 🧪 測試案例

### 測試案例 1: 平日收據

```
輸入：
- 日期：2025-02-17（週一）
- 金額：€100
- 幣別：EUR

預期：
- 查詢日期：2025-02-17
- 匯率：~34.5（實際查詢）
- 台幣：~3,450
```

---

### 測試案例 2: 週六收據

```
輸入：
- 日期：2025-02-15（週六）
- 金額：£200
- 幣別：GBP

預期：
- 查詢日期：2025-02-14（週五）
- 匯率：~40.2（實際查詢）
- 台幣：~8,040
```

---

### 測試案例 3: 手動修正日期

```
初始：
- 日期：2025-02-10
- 金額：$100
- 幣別：USD
- 匯率：31.5
- 台幣：3,150

修正後：
- 日期：2025-02-10 → 2025-02-15

預期：
- 重新查詢匯率（2025-02-14，週五）
- 匯率：31.5 → 31.8（假設）
- 台幣：3,150 → 3,180（自動更新）
```

---

### 測試案例 4: 手動修正金額

```
初始：
- 日期：2025-02-15
- 金額：¥1,000
- 匯率：0.220
- 台幣：220

修正後：
- 金額：¥1,000 → ¥1,500

預期：
- 匯率：保持 0.220
- 台幣：220 → 330（自動更新）
```

---

## 📋 部署檢查清單

- [ ] 上傳 app_enhanced_v2.py（重新命名為 app_enhanced.py）
- [ ] 上傳 countries_master.json
- [ ] 上傳 requirements.txt（確認包含 yfinance）
- [ ] 推送至 GitHub
- [ ] 等待 Streamlit Cloud 重新部署
- [ ] 測試基本功能
- [ ] 測試匯率查詢（平日）
- [ ] 測試匯率查詢（週末）
- [ ] 測試手動修正連動
- [ ] 測試照片預覽

---

## ⚠️ 注意事項

### 匯率資料來源

**使用：** Yahoo Finance API（透過 yfinance）

**限制：**
- 歷史資料最多往前查詢 7 天
- 部分貨幣對可能無資料
- 假日資料會自動往前推

**預設值：**
- 如果無法查詢到匯率，使用預設值 35.0

---

### Session State 管理

**新增狀態：**
```python
st.session_state['uploaded_images'] = {}  # 儲存圖片
```

**清空時機：**
- 使用者點擊「清空辨識紀錄」
- 成功同步至雲端後

---

## 🎉 總結

### v4.8.0 核心價值

1. **準確性提升**
   - 根據收據日期查詢正確匯率
   - 週末自動處理

2. **效率提升**
   - 即時顯示台幣金額
   - 修正後自動重新計算

3. **使用體驗提升**
   - 照片預覽功能
   - 雙欄佈局更清晰

---

**版本：** v4.8.0  
**發布日期：** 2025-02-13  
**核心功能：** 智慧匯率查詢、即時連動計算、照片預覽
